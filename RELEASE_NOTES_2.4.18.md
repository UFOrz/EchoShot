# EchoShot 2.4.18

## RunningHUB

- 新增独立的 RunningHUB 国内站模板，反推、生图、上传、任务查询和工作流均使用国内站官方接口，并支持识别常见的国内站地址写法。
- 国内站不再预置默认模型；国际站也不再固定显示旧的 35 个条目。两个站点均可从 LLM 模型目录和 RunningHUB 官方标准模型注册表自动获取当前模型。
- 重新获取会恢复官方模型及能力信息，同时保留仍然存在的手动勾选和自定义别名；获取结果会分别显示总数、反推数和生图数。
- 普通自定义接口返回网页而不是模型 JSON 时，会明确提示检查 Base URL 或改用对应的内置模板。
- RunningHUB 国内站的 Get Key 地址已更新为新的邀请链接。

## 模型列表

- 为能够可靠判断用途的模型显示线条能力图标：放大镜表示适合反推，相机表示适合文生图，图片框表示适合图像编辑。
- 能力图标只作提示，不会自动勾选“反推”或“生图”；自定义或未知模型在缺少可靠信息时不显示标记。
- 修复勾选模型能力后列表自动跳回顶部的问题，操作后会保持当前位置。

## 升级提示

- 从旧版本升级后，RunningHUB 国内站原有的静态默认模型会被清理，请在设置页点击一次“自动获取模型”。
