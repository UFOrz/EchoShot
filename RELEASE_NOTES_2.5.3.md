# EchoShot 2.5.3

- RunningHUB 工作流提交前会核对标记节点的实际输入字段；若 `text` 已不存在且能唯一识别 `prompt` 等文本字段，会使用实际字段名。
- 如果字段无法确定，提交前即提示节点 ID 和现有字段，避免继续发送已知错误的 `nodeInfoList`。
- 平台仍返回节点不匹配时，提示重新导出工作流 API JSON 核对节点配置。此改动不保证所有自定义工作流都可直接生成。
